# TODO: Change Font to Poppins

- [x] 1. Update `index.html`: Add Google Fonts Poppins link and Tailwind v4 default font config.
- [x] 2. Update `src/index.css`: Remove conflicting Tailwind import, keep Poppins, apply global font.
- [x] 3. Update `src/main.jsx`: Uncomment `import "./index.css"`.
- [x] 4. Update `src/assets/tailwind.css`: Fix `@font-face` source path typos.
- [x] 5. Update `src/Layout/MainLayouts.jsx`: Fix `font-ta` typo to `font-poppins`.
- [x] 6. Run dev server to verify changes.

